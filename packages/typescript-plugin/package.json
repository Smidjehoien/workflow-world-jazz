{
  "name": "@vercel/workflow-typescript-plugin",
  "version": "0.0.1-alpha.3",
  "description": "TypeScript Language Service Plugin for Vercel Workflow SDK",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "tsc --build --clean && rm -r dist ||:",
    "test": "vitest run src",
    "test:watch": "vitest watch src",
    "test:coverage": "vitest run src --coverage",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "typescript",
    "language-service-plugin",
    "workflow",
    "vercel"
  ],
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "dependencies": {
    "builtin-modules": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vercel/workflow-core": "workspace:*",
    "@vercel/workflow-tsconfig": "workspace:*",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
