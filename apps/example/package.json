{
  "name": "@workflow/example-app",
  "private": true,
  "type": "module",
  "version": "0.0.2-alpha.8",
  "scripts": {
    "workflow": "workflow",
    "wf": "wf",
    "build": "workflow build --target vercel-build-output-api"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "@vercel/workflow-cli": "workspace:*",
    "esbuild": "^0.25.5"
  },
  "dependencies": {
    "@vercel/functions": "^2.2.2",
    "@vercel/workflow-core": "workspace:*",
    "ai": "beta",
    "mixpart": "^0.0.4",
    "zod": "3"
  }
}
