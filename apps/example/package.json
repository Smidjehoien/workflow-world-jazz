{
  "name": "@workflow/example-app",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "workflow build && mkdir -p public && echo 'Hello, world!' > public/index.html",
    "clean": "rm -r public; rm -r api/generated ||:",
    "invoke": "node invoke.mts"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "@vercel/workflow-cli": "workspace:*",
    "esbuild": "^0.25.5"
  },
  "dependencies": {
    "@vercel/functions": "^2.2.2",
    "@vercel/workflow-core": "workspace:*"
  }
}
