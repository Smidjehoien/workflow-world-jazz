/**
 * TODO: Implement a VM that can be used to run the user's workflow code.
 * 
 * The purpose of the VM is to provide a deterministic execution environment
 * for the user's workflow code, so APIs like `new Date()` and `Math.random()`
 * are deterministic.
 */
