---
"@vercel/workflow-world-testing": patch
---

create a `@vercel/workflow-world-testing` package that will provide "spec test" for a world implementation

this is currently taking a package name which doesn't make it configurable, so maybe in the future we can also
support a `World` instance to be tested directly.
