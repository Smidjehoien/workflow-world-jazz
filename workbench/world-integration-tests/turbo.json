{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src", "workflows"],
      "outputs": [".well-known"]
    },
    "test": {
      "dependsOn": ["build"]
    }
  }
}
